import { StyledSearchSettings } from './SearchSettings.style';

export interface SearchSettingsProps {
  // TODO: Document component props
}

/**
 * TODO: document component functionality
 *
 * [Figma](https://https://www.figma.com/file/...)
 *
 * ```tsx
 * <SearchSettings />
 * ```
 */
export function SearchSettings(props: SearchSettingsProps) {
  return <StyledSearchSettings>SearchSettings</StyledSearchSettings>;
}
