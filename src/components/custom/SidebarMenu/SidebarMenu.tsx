import { StyledSidebarMenu } from './SidebarMenu.style';

export interface SidebarMenuProps {
  // TODO: Document component props
}

/**
 * TODO: document component functionality
 *
 * [Figma](https://https://www.figma.com/file/...)
 *
 * ```tsx
 * <SidebarMenu />
 * ```
 */
export function SidebarMenu(props: SidebarMenuProps) {
  return <StyledSidebarMenu>SidebarMenu</StyledSidebarMenu>;
}
