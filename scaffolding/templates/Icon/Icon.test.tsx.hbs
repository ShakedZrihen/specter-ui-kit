import { {{normalizeIconName iconName}}Icon } from './{{normalizeIconName iconName}}Icon';
import { render } from '@testing-library/react';
import { GitstreamTheme } from '@/context/theme';

describe('{{normalizeIconName iconName}}Icon', () => {
  it('should render svg', () => {
    const { container } = render(
      <GitstreamTheme>
        <{{normalizeIconName iconName}}Icon />
      </GitstreamTheme>,
    );
    expect(container.getElementsByTagName('svg')).toBeDefined();
  });
});
